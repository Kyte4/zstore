{
  "name": "zstore",
  "private": true,
  "scripts": {
    "format": "npm run format --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "workspaces": [
    "server/app",
    "server/client"
  ]
}
